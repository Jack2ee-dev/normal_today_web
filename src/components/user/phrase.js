import React from 'react';
import styled from 'styled-components';

import { StyledPhrase } from './styled';

const Phrase = () => {
  return (
    <StyledPhrase>당신에 대해 더 알려주세요</StyledPhrase>
  );
};

export default Phrase;
